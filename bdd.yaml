apiVersion: apps/v1
kind: Deployment
metadata:
  name: 
spec:
  replicas: 1
  selector:
    matchLabels:
      name: 
  template:
    metadata:
      labels:
        name: 
    spec:
      containers:
        - name: postgres
          image: postgres:17-alpine
          ports:
            - containerPort: 5432
          env:
            - data

--- 
apiVersion: v1
kind: Service
metadata:
  name: _service
spec:
  selector:
    name: 
  ports:
    - protocol: TCP
      port: 
      targetPort: 